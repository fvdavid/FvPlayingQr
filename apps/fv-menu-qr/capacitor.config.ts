import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FvMenuQr',
  webDir: '../../dist/apps/fv-menu-qr',
  bundledWebRuntime: false,
};

export default config;
